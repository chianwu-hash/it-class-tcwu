# 校內考卷轉文字規則

- `raw/` 保留原始檔，不覆蓋、不改內容。
- `text/` 放工作用 `UTF-8` 文字檔，供後續命題風格分析使用。
- 目前支援格式：`pdf`、`docx`。
- `pdf` 以頁為單位抽字，保留 `--- PAGE N ---` 分隔。
- `docx` 以段落為單位抽字。
- 若遇到圖片題、表格題或版面歧義，分析時需回頭核對 `raw/` 原檔。
- 若轉出的文字品質不足，不直接硬用，應在對應科目的 `notes.md` 記錄問題。
